function getAllCaptureGroups({ str, regExp }) {
  const reg = new RegExp(regExp)
  const arr = []
  let a = null
  /* eslint-disable */
  while (a = reg.exec(str)) {
    arr.push(a)
  }
  /* eslint-enable */
  return arr
}

export default { getAllCaptureGroups }
