import assert from 'power-assert'
import {
  isArray,
} from '../src/lang'

describe('test util/lang', () => {
  it('lang/isArray', () => {
    // Given
    const array = []
    const number = 10
    // Then
    assert(isArray(array) === true)
    assert(isArray(number) === false)
  })
})
