import assert from 'power-assert'
import {
  sendFeedbackTextMessage,
} from '../src/feedback'

describe('test feedback', () => {
  it('sendFeedbackTextMessage', async () => {
    // Given
    const data = await sendFeedbackTextMessage({
      doctorId: '2016145',
      message: '你好。世界',
    })
    // Then
    assert(data)
  })
})
