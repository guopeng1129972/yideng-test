function isArray(o) {
  return Object.prototype.toString.apply(o) === '[object Array]'
}

export default (obj, keys) => (isArray(obj) ? obj : [obj]).every(each => keys.every(k => each[k] !== undefined))
