import assert from 'power-assert'
import { getContentInfoByPatientIds } from '../src/medclips'

describe('test medclips', () => {
  it('根据患者id获取患者填写的表单', async () => {
    // Given
    const patientIds = [540958]
    const resp = await getContentInfoByPatientIds({
      patientIds,
    })
    console.log(resp)
    // Then
    assert(resp)
  })
})
