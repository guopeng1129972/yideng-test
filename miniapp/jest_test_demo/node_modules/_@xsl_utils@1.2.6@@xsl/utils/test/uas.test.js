import assert from 'power-assert'
import { getUserInfoByIds } from '../src/uas'

describe('test uas', () => {
  it('get userInfo by userIds', async () => {
    // Given
    const userIds = [2084315980]
    const userInfoMap = await getUserInfoByIds(userIds)
    // Then
    assert(userInfoMap[2084315980])
  })
})
